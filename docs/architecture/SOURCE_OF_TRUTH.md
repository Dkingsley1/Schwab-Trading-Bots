# System Source Of Truth

This file is the top-level source-of-truth map for the Schwab trading bot. Each row names the canonical artifact to trust first, the generated evidence that verifies it, and the owner surface that should be edited when behavior changes.

| Area | Canonical Source | Verification / Runtime Evidence | Edit Here First |
| --- | --- | --- | --- |
| Operator commands | `scripts/ops/commands_hygiene_bot.py` inventory | `COMMANDS.md`, `governance/health/commands_contract_latest.json` | `scripts/ops/commands_hygiene_bot.py` |
| Platform defect closure | `docs/operations/PLATFORM_HARDENING_LEDGER.md` and its listed owners | Reproduction tests, current producer evidence, explicit operational prerequisites | Fix the source, then verify consumers and deployment; implementation, runtime availability, and live qualification remain separate |
| Release completion criteria | `docs/operations/PLATFORM_COMPLETION_CHECKLIST.md`, detailed definitions in `docs/operations/PLATFORM_COMPLETION_WORK_PACKAGES.md`, and each subsystem's existing owner | Stable requirement IDs, source owners, release identity, tests, deployment adoption, operational evidence, and explicit approval prerequisites | The checklist owns status and the work packages define acceptance; track open defects separately from unproven existing behavior, and keep collection, unattended readiness, research qualification, and live promotion independent |
| Runtime training snapshot publication | `scripts/build_runtime_training_snapshot.py`, `core/runtime_training_common.py`, `scripts/ops/readiness_evidence_refresh.py` | Total worker deadline, phase diagnostics, atomically published rows/health, and matching schema-v2 row digests | Builder and readers together; interrupted or mixed generations fail closed, partial scan coverage stays explicit, and timeouts never refresh evidence or qualify a candidate |
| Recovery admission and security evidence truth | `scripts/daily_state_snapshot_drill.py`, `scripts/ops/sqlite_reclaim_control.py`, `scripts/ops/storage_disaster_recovery.py`, `scripts/security_hardening_audit.py` | Combined copy/scratch budgets, owned storage lock, verified retention, required manifest digests, producer timestamps, actual Git hook route | Preserve failed/unverified data; metadata-only files, nonparticipating resource writers, uninstalled hooks, and unknown scan results remain visible debt rather than readiness credit |
| Evidence publication and recovery scope | `scripts/ops/long_runtime_common.py`, `scripts/ops/readiness_evidence_refresh.py`, `scripts/daily_state_snapshot_drill.py`, `scripts/ops/storage_disaster_recovery.py` | Producer timestamps, published artifact status, snapshot/restore hashes, SQLite checks, separate control-drill and RTO fields | No file-mtime freshness, metadata-only restore credit, stdout-only success, or simulated timing promoted to production restore proof |
| Operator livefeed status | `scripts/ops/live_feed_status_contract.py`, `scripts/ops/live_feed_tail.sh` | freshness, reconciliation, cause, recovery, PAPER impact, and action fields in each livefeed status snapshot | livefeed status contract first, then the owning health producer |
| Report opening and PDF fallbacks | `scripts/ops/open_report_artifact.sh` | `exports/reports/*_latest.{pdf,html,md}` | `scripts/ops/open_report_artifact.sh` |
| Broker truth and account readiness | `scripts/run_shadow_training_loop.py` broker-truth helpers | `governance/health/broker_truth_*_latest.json`, `governance/health/broker_readiness_latest.json` | broker adapter and broker-truth helpers |
| Schwab auth handshake | `scripts/ops/schwab_auth_refresh.py`, `scripts/ops/schwab_auth_supervisor.py` | `governance/health/schwab_auth_refresh_latest.json`, `governance/health/schwab_auth_supervisor_latest.json` | auth helper scripts |
| Schwab credential, refresh-token, and auth-epoch lifecycle | `core/brokers/schwab_credentials.py`, `scripts/ops/premarket_token_guard.py`, `core/broker_auth_epoch.py`, runtime loader policy in `scripts/ops/load_runtime_env.sh` | Keychain readiness flag, serialized noninteractive refresh-grant receipt, nonsecret auth epoch in refresh/launcher artifacts, and post-refresh dependency receipt | the central credential resolver first; production runtime may strip secret environment variables only after Keychain readiness is proven, every direct refresh path must use the same resolver, refresh-token rotation is singleton-locked and atomic, long-lived Schwab clients recycle or rebind after a token epoch change, and interactive browser OAuth is reserved for refresh-token failure rather than routine access-token renewal |
| Auth-dependent broker and paper reconciliation | `scripts/ops/schwab_auth_post_refresh.py`, `scripts/ops/schwab_account_snapshot_refresh.py`, `scripts/ops/paper_execution_truth_layer.py` | `governance/health/schwab_auth_post_refresh_latest.json`, fresh account snapshot, broker truth, account-position studies, and paper truth | preserve the fixed token, lease, account, position, portfolio, and paper-truth dependency order; the recovery path is read-only/paper-only, uses bounded Schwab request and retry budgets, and may never continue from stale pre-auth account evidence |
| Sleeve performance metrics | `scripts/paper_performance_report.py` | `governance/health/paper_performance_latest.json`, `exports/reports/paper_performance_latest.*` | paper performance report; lifetime flow, current-day flow, all-candidate research flow, active-stage promotion flow, active inventory, and the identity-stamped developmental generation ledger are separate accounting scopes; `candidate_research_forward_flow` is diagnostic, while only exact-cohort `candidate_forward_flow` may grade promotion |
| Generation-aware developmental profitability learning | `config/profitability_self_assessment_v1.json`, `scripts/ops/profitability_self_assessment.py`, candidate events owned by `scripts/ops/production_excellence_control.py` | `developmental_generation_flows` in `governance/health/paper_performance_latest.json`, `developmental_soak_learning` in `governance/health/profitability_self_assessment_latest.json`, `[profitability-truth]` livefeed fields, system-needs routes, and the continuous-soak developmental contract | verified accepted generations are joined to identity- and time-bound post-cost outcomes; earlier generations may prioritize bounded paper collection, replay, independent fills, weak-sleeve containment, and loss labeling, but mixed/unbound history remains diagnostic, association is not causation, and no historical generation can raise the current economic grade, earn current scope-validation credit, force trades, increase loss-recovery size, loosen thresholds directly, promote, or enable live execution |
| Cumulative-soak generation behavior attribution | `scripts/ops/generation_behavior_attribution.py`, hash-chained candidate events, candidate-stamped decision rows, and candidate-bound paper generation flows | `governance/research/generation_behavior_attribution_latest.json`, `exports/reports/operator/generation_behavior_attribution_latest.md`, continuous-soak attribution summary, and self-model awareness | candidate ID, generation, and immutable event windows are verified before comparison; pre-stamping legacy rows may be associated descriptively by accepted time window but remain explicitly non-promotion-grade, missing evidence stays missing, duplicate/archive copies are suppressed, and all behavior and economic deltas remain associational rather than causal or profitability guarantees |
| Candidate scope-validation policy | `config/candidate_scope_validation_v1.json`, `scripts/ops/candidate_scope_validation.py`, and accepted scope clocks owned by `scripts/ops/production_excellence_control.py` | scope rows in `governance/health/production_excellence_control_latest.json`, `governance/health/continuous_soak_integrity_control_latest.json`, live-money/canary contracts, investor readiness, and `[soak]` livefeed fields | operations use 72 hours plus three XNYS sessions; data/dependencies use 120 plus five; promotion uses 336 plus ten; strategy/execution/risk retain 720 plus twenty; both measures are required, planned-maintenance downtime and interrupted sessions earn no credit, unknown scopes fail closed at the material tier, cumulative history remains separate, and the policy never grants order authority |
| Current-generation historical fill learning | `config/generation_fill_learning_v1.json`, `scripts/ops/generation_fill_learning.py`, hash-chained candidate events, and exact candidate or decision receipts | `governance/research/generation_fill_learning_latest.json`, compact per-target learning and quarantine JSONL files, and the guarded master/Grand Master trainer handoff | the current candidate owns the derived challenger run while every fill retains its source generation; timestamp-only relabeling is forbidden, unbound rows are quarantined, simulated fills cannot prove empirical profitability, losses receive bounded hard-negative weight, recency and generation budgets prevent dominance, purged chronological and leave-one-generation-out validation are mandatory, and no output can swap runtime models, promote, grade the current candidate, earn soak credit, submit an order, or enable live execution |
| Persistent paper recovery balance | `scripts/ops/paper_profitability_control.py`, runtime enforcement in `scripts/run_shadow_training_loop.py` | `paper_debt_recovery_contract` in `governance/health/paper_profitability_control_latest.json` and `governance/health/paper_runtime_profitability_controls_latest.json`, `[paper-debt]` livefeed row, live-canary and live-money readiness gates | the first authoritative negative active-book snapshot establishes an immutable baseline; candidate cumulative post-cost PnL is carried exactly once across generations, active-book and attribution views must agree, stale or regressed evidence fails closed, and recovery cannot force trades, martingale, average down, or raise size because of loss |
| Candidate-bound sleeve and strategy paper scaling | `scripts/ops/paper_profitability_control.py`, identity metadata from `core/sleeve_strategy_specialization.py`, runtime enforcement in `scripts/run_shadow_training_loop.py` and `core/profitability_hardening.py` | `sleeve_strategy_profitability_scaling_contract` in both paper profitability artifacts, `[profit-scaling]` livefeed row, live-canary and live-money readiness gates | only an already-authorized paper `BUY` may be scaled; probation and incomplete evidence deweight, validated independent evidence may advance from `1.00` to at most `1.10`, candidate/source/specialization mismatch fails closed, debt/quarantine and all portfolio/execution limits win, `SELL` and reduce-only exits stay open at full requested size, and the contract has no live-order or profitability-claim authority |
| Paper execution authority | `scripts/ops/paper_live_data_standard.py`, `core/profitability_hardening.py`, final enforcement in `core/base_trader.py` | `governance/health/paper_live_data_standard_latest.json`, hash-bound generated registry candidate, staged promotion receipt, and source-write guard | paper standard first; legacy enable flags are observation-only, execution needs explicit market-signal authority or bounded probation, and new exposure must also match the one active profile, sleeve, symbol, and strategy stage; the broad fleet remains collecting/shadow, historical positions may reduce or close without crossing flat, and stage advancement is manual |
| Paper-execution runtime breaker and collection continuity | `scripts/run_all_sleeves.py`, `scripts/ops/process_watchdog.py`, `scripts/ops/runtime_gate_dashboard.py` | `governance/health/all_sleeves_launcher_latest.json`, launcher readiness contract, watchdog execution-guard fields, dashboard execution-runtime section | a breaker trip latches only the execution group while collection continues; elapsed cooldown alone cannot restart the executor, and release requires fresh authoritative measurements produced after the current broker auth epoch with no remaining breaker reason; `guarded_ready` means supervised collection is healthy while paper execution is visibly safety-parked, not fully ready and not crashed |
| Candidate-bound independent fills | `scripts/ops/market_replay_fill_capture.py`, `scripts/ops/independent_fill_evidence_acquisition.py` | `governance/health/market_replay_fill_capture_latest.json`, immutable records under `governance/evidence/independent_fill_records`, candidate trade logs under `exports/trade_logs/independent_fills` | replay producer first, then acquisition normalizer; delayed-quote replay is not a broker receipt and never grants promotion or order authority |
| Historical profitability crisis drills | `config/profitability_crisis_drill_v1.json`, versioned definitions under `config/stress_scenarios`, `scripts/ops/profitability_crisis_drill.py` | `governance/research/profitability_crisis_drill_latest.json`, scenario and policy SHA-256 receipts, candidate-state before/after receipt | policy and official event anchors first, then deterministic stress parameters and runner; the 2008 GFC, 2020 pandemic-liquidity break, and 2023 regional-bank run test cost-adjusted action ranking, stressed fills, severe-phase abstention, reduce-only exits, and recovery re-entry without broker, paper-order, live-order, promotion, candidate-mutation, label, history-rewrite, or profitability-claim authority; diagnostic stress parameters are not reconstructed historical ticks or organic candidate evidence |
| Adversarial profitability and capital-capacity drills | `config/profitability_adversarial_drill_v1.json`, `scripts/ops/profitability_adversarial_drill.py` | `governance/research/profitability_adversarial_drill_latest.json`, fourteen scenario receipts, candidate-state before/after receipt, full-fleet capacity curves | policy first, then the bounded runner; all 111 runtime sleeves are classified, 25 control-only sleeves receive no fake economics, and 86 trading sleeves receive twenty-two diagnostic capital tiers from $200 through a $1,000,000,000 long-range endpoint under six market states with unit feasibility, risk-limited notional, impact, costs, participation, breakpoints, and strategy-inheritance metadata; the suite starts no daemon, contacts no broker, submits no order, cannot tune thresholds, and its A+ is control coverage rather than organic evidence, certified deployable capacity, promotion, live release, or a profitability guarantee |
| Candidate-bound paper behavior interventions | `config/paper_behavior_intervention_drill_v1.json`, `core/paper_behavior_interventions.py`, proposal runner `scripts/ops/paper_behavior_intervention_drill.py`, single runtime writer `scripts/ops/paper_profitability_control.py`, final paper enforcement in `scripts/run_shadow_training_loop.py` | `governance/research/paper_behavior_intervention_drill_latest.json`, `paper_behavior_intervention_overlay_contract` in `governance/health/paper_runtime_profitability_controls_latest.json`, intervention receipts and tags in paper decision/execution evidence | policy and evaluator first, then the zero-authority drill proposal and the existing profitability single writer; all fourteen scenarios and seventeen cases must pass with a fresh exact candidate and policy binding before a 24-hour `paper_probation` overlay can be admitted; the overlay may only preserve or reduce an already-authorized paper BUY, preserves HOLD/SELL/reduce-only exits, fails to a no-op on mismatch or expiry, and cannot originate/reverse/enlarge, submit orders, mutate candidates, tune policy, grant promotion/live authority, or convert synthetic drill economics into organic profitability evidence |
| Unified trading behavior drill program | `config/trading_behavior_drill_program_v1.json`, `scripts/ops/trading_behavior_drill_program.py`, single runtime writer `scripts/ops/paper_profitability_control.py` | `governance/research/trading_behavior_drill_program_latest.json`, compact receipts under `governance/research/trading_behavior_drill_runs/*.json`, the three detailed suite artifacts | program policy and locked conductor first, then the existing single runtime writer; one run freezes candidate and policy receipts, serially executes crisis, adversarial, and intervention suites under bounded resources, stops on candidate mutation, compares only same-input complete runs, and rejects partial, stale, regressed, over-budget, or authority-bearing evidence; the program may propose a risk-reducing paper overlay but cannot write runtime controls, submit orders, promote, change allocation, relax risk, enable live execution, or turn diagnostics into profitability claims |
| Decision and signal evidence | `core/accountability.py` | `decision_explanations/**`, `governance/channels/decision/**`, `governance/events/signal_generation_*.jsonl` | shared accountability writer |
| Hierarchical institutional decision quality and paper/live parity | `config/institutional_decision_flow_v1.json`, `core/institutional_decision_flow.py`, candidate-forward statistics in `scripts/paper_performance_report.py`, `scripts/multiple_testing_guard.py`, and `scripts/decay_monitor.py`, paper hook in `scripts/run_shadow_training_loop.py`, enforcement in `core/execution_lane_pipeline.py` | deterministic family/profile strategy and playbook receipts, stage-outcome traces, shared live-feed operator summaries, position-aware action semantics, candidate-bound direct-versus-proxy quantitative evidence, evaluation digests, paper/live execution guards, `governance/research/institutional_decision_flow/latest.json`, bounded compact history | policy and canonical evaluator together; every specialized sleeve resolves to a complete hash-bound horizon, role, edge, entry, exit, sizing, regime, cost, uncertainty, capacity, validation, shorting, transition, stage-priority, and mode-progress contract; summary and playbook digests prevent telemetry drift and stale-intent reuse, paper may collect missing evidence but direct adverse evidence only reduces risk, while future live requires fresh direct passing family evidence and a legal account-position transition; the layer cannot create/reverse/enlarge/bypass/submit/grant promotion and the detached `shadow_research/**` sidecar remains read-only |
| Candidate-bound quantitative challenger research | `config/quantitative_challengers_v1.json`, `core/quantitative_challengers.py`, `scripts/quantitative_challenger_report.py` | `governance/research/quantitative_challenger_latest.json`, compact per-profile metadata in institutional decision rows and livefeed | policy, deterministic methods, and report builder together; all eight methods consume only identity-matched candidate-forward post-cost data, run outside the hot path under bounded seeded workloads, and remain read-only metadata with no action, sizing, allocation, training-label, paper/live-order, candidate-mutation, history-rewrite, or promotion authority; implementation availability and economic support are separate truths |
| Candidate-bound alpha lifecycle and cross-sleeve residual ownership | `config/alpha_generation_control_v1.json`, `core/alpha_evidence_contract.py`, `core/causal_attribution.py`, `scripts/ops/alpha_generation_control.py` | candidate binding and conservative net-edge contracts in decision/execution evidence, `governance/health/alpha_generation_control_latest.json`, `exports/reports/operator/alpha_generation_control_latest.md`, and the alpha expansion gate in strategy-generation evidence | policy and evidence control together; regime, liquidity, macro, factor, risk, and cost context may be shared, but trade logic is not duplicated; one evidence-qualified owner is selected per symbol/horizon/alpha-family exposure from positive candidate-bound residual LCB after FDR, deflated-Sharpe, PBO, cost, regime, and correlation gates; incomplete evidence returns cash and freezes new offspring proposals while existing collection continues; the layer cannot change actions, sizes, allocation, promotion, orders, history, or live authority |
| Candidate-bound alpha concept ontology, measurement laboratory, and all-sleeve toolbox | `config/alpha_concept_registry_v1.json`, `config/sleeve_alpha_toolbox_v1.json`, `core/alpha_concept_engine.py`, `scripts/ops/alpha_concept_report.py`, `scripts/ops/sleeve_alpha_toolbox_control.py` | `governance/research/alpha_concept_report_latest.json`, `governance/research/sleeve_alpha_toolbox_latest.json`, operator Markdown, compact self-model awareness, and the `alpha-concepts` and `sleeve-alpha-toolbox` commands | registry, deterministic estimators, report builder, and sleeve router together; the finite ontology covers materially useful concepts without claiming universal enumeration, sixteen engines include time-ordered conformal calibration, Page-CUSUM drift, residual redundancy, regime lower bounds, and cost-stress survival, every declared sleeve resolves without a generic fallback and receives tools for every required evidence axis, while implementation and route coverage remain separate from current-candidate evidence, post-cost economic support, and profitability; the layer has no action, sizing, allocation, label, paper/live-order, promotion, candidate-mutation, history-rewrite, or profitability-guarantee authority |
| Sleeve strategy contracts, contextual economic value, 12,000-strategy lineage, 1,989-record family catalog, regime relevance, quality verdicts, identity, attribution, and lifecycle | `config/sleeve_strategy_contracts_v1.json`, `core/sleeve_strategy_specialization.py`, `scripts/sleeve_strategy_specialization_report.py`, `scripts/strategy_library_query.py` | `governance/research/sleeve_strategy_contracts_latest.json`, `governance/research/sleeve_strategy_specialization_latest.json`, `governance/research/sleeve_strategy_library_latest.json`, `governance/research/sleeve_strategy_families_latest.json`, strategy receipts in decision and paper-performance metadata | policy and resolver together; `sleeve_economic_context_v1` requires every trading objective to earn positive post-cost objective-specific economic value in a supported regime before separate capital consideration, while hedge, preservation, execution, and control objectives retain their correct utility definitions; `active`, `collect_only`, `quarantined`, `retired`, and `control_only` are canonical economic states, all sleeves need not activate or profit simultaneously, and non-active or control-only states cannot receive capital consideration; all 111 sleeves receive 108 or 109 objective-appropriate hypotheses for exactly 12,000, while only the 879 hot contracts reach runtime ranking and the other 11,121 remain cold; current point-in-time regime evidence annotates relevance and gates reviewed cold admission, good/bad verdicts require mature candidate-bound objective-aware evidence, and the layer cannot create, reverse, enlarge, allocate, relabel, rewrite history, promote, submit an order, or guarantee profitability |
| Training labels and outcome authority | `core/runtime_training_common.py`, `scripts/build_behavior_dataset_from_decisions.py`, `scripts/ops/training_labeling_intelligence.py` | `governance/training_labeling_intelligence/all_bot_label_materialization_latest.json`, behavior-dataset label contract, runtime training diagnostics `label_evidence_audit` | shared runtime label guard and labeling intelligence together; candidate and generation lineage remain exact, and post-cost BUY/SELL/HOLD counterfactuals at 5m, 1h, and 1d are materialized separately from the observed action |
| Immutable training lineage and replay horizons | `scripts/ops/training_lineage_manifest.py`, `scripts/experiment_tracker.py`, `scripts/feature_store_manifest.py`, `scripts/snapshot_coverage_sentinel.py`, `scripts/paper_replay_drill.py` | `governance/health/training_lineage_manifest_latest.json`, `governance/health/snapshot_coverage_training_latest.json`, `governance/health/paper_replay_training_latest.json`, signed experiment registry and immutable ledger in plain or lossless gzip form | keep the 2-hour operational snapshot and 24-hour training snapshot contracts separate; training replay uses a rolling 14-day real-paper window, archived JSONL remains readable after compaction, and a lineage score never substitutes for confirmed training, promotion quality, or live release |
| Global central-bank context and cross-source routing | `config/global_central_bank_registry_v1.json`, `core/global_central_bank_context.py`, `scripts/collect_global_central_bank_context.py`, `scripts/synchronize_global_central_bank_context.py` | `exports/external_context/global_central_bank_context_latest.json`, `exports/external_context/central_bank_cross_source_latest.json`, matching health artifacts and append-only point-in-time history | registry and source contracts first, then the synchronizer; a raw bank row cannot self-certify a join, future/stale/conflicted dimensions cannot route, and this layer has no order or promotion authority |
| Public macro and microeconomic source inventory | `config/economic_source_registry_v1.json`, `core/economic_source_registry.py`, source-specific collector contracts | `governance/health/economic_source_inventory_latest.json`, `economic-source-inventory` command, expanded 32-bank member list, source-to-producer/capability/plane/family routes, and registry receipt | registry first when adding, moving, or retiring an economic source; every row must name a real producer and declared capability, official HTTPS provenance, macro/micro scope, and bounded consumers; source count is inventory rather than alpha/readiness, optional failures remain isolated, missing dimensions are omitted, and the registry has no order or promotion authority |
| Classified official public financial context | `config/public_financial_context_routing_v1.json`, `scripts/collect_public_financial_context.py`, `scripts/collector_contracts.py` | `exports/external_context/public_financial_context_latest.json`, `governance/health/public_financial_context_sync_latest.json`, taxonomy, economic-registry, and snapshot receipts, field-level capability proofs, and classified routes in the decision-context mesh | source contracts and routing taxonomy first; SEC issuer facts, OFR stress, FDIC failure events and quarterly bank aggregates, Federal Register policy activity, ECB euro funding evidence, and New York Fed primary-dealer balance-sheet/funding evidence are classified by scope, domain, cadence, semantic direction, plane, family, and authority; source adapters fail independently, future rows and unclassified features are quarantined, supplemental failures cannot lower baseline paper status, missing optional context is omitted rather than zero-filled, and this layer has no action, sizing, order, promotion, or profitability-claim authority |
| Twelve-plane macro/micro decision context | `config/decision_context_mesh_v1.json`, `core/decision_context_mesh.py`, `scripts/collect_decision_context_mesh.py` | `exports/external_context/decision_context_mesh_latest.json`, `governance/health/decision_context_mesh_latest.json`, append-only compressed history, runtime and behavior-schema routing, source-verification row | plane contract and official-source parsers first, then routing; missing or future observations are omitted, grades remain evidence-derived, and context has no execution or promotion authority |
| Collector mesh, capability materialization, decision-aligned routing, and organic evidence accrual | `scripts/collector_contracts.py`, `config/capability_materialization_v1.json`, `config/collector_capability_catalog_v1.json`, `config/sleeve_ingestion_routing_v2.json`, `config/institutional_decision_flow_v1.json`, `core/capability_materialization.py`, `core/collector_capability_routing.py`, `core/collector_transport.py`, `scripts/provider_mesh_control.py`, `scripts/ops/sleeve_ingestion_production_control.py` | `governance/health/collector_contracts_latest.json`, `governance/collector_capabilities/materialized_capabilities_latest.json`, `governance/health/collector_capability_control_latest.json`, `governance/collector_capabilities/bot_subscriptions_latest.json`, signed route context in decision traces and livefeed, `governance/health/provider_mesh_latest.json` | source definitions and direct proofs first, then the logical catalog and family-specific route policy; paper-required and live-required capability floors stay separate, producer selection is quality scored with independent failovers, transport is bounded and receipt signed, unsupported enrichment remains explicit promotion debt, and routing has zero strategy, order, registry-mutation, or promotion authority |
| Eight-stream research-context expansion | `core/research_context_expansion.py`, `scripts/collect_research_context_expansion.py`, `config/collector_capability_catalog_v1.json`, `core/runtime_training_common.py` | eight independent external-context and health artifacts plus `governance/health/research_context_expansion_latest.json`, capability proof receipts, and freshness-checked runtime features | one bounded process materializes cross-asset breadth, tape liquidity, options surface, futures curve, earnings, portfolio risk, FINRA fixed-income, and BIS global-liquidity context; each capability requires direct or source-backed evidence, default zeros cannot prove missing futures dimensions, stale or receipt-invalid payloads are rejected, source failure serves last-good only inside its freshness contract, and the entire layer has no action, sizing, paper/live-order, registry-mutation, or promotion authority |
| Bot hierarchy, multi-axis regimes, and correlated voting boundaries | `config/bot_organization_v1.json`, `core/regime_taxonomy.py`, `core/bot_organization.py`, `core/hierarchical_ensemble.py` | `governance/health/bot_organization_latest.json`, `governance/bot_organization/bot_hierarchy_latest.json` | organization and regime policy first; the read-only hierarchy now supplies paper consensus sleeve, sub-sleeve, and correlation identities, while regime compatibility remains observational and neither layer grants paper or live authority |
| Sleeve-master and Grand Master evidence synthesis | `config/master_grandmaster_evidence_v2.json`, `core/master_grandmaster_evidence.py` | `governance/health/master_grandmaster_evidence_v2_latest.json`, `governance/master_grandmaster/evidence_packets_v2_latest.json` | evidence policy and synthesis engine together; the layer is advisory, shadow-only, and has no order or automatic-promotion authority |
| Storage routing and tiered ingestion lifecycle | `core/storage_router.py`, `core/storage_target_override.py`, `config/tiered_ingestion_lifecycle_v1.json`, `core/tiered_ingestion_lifecycle.py`, `scripts/storage_tier_policy.py` | `governance/health/storage_*_latest.json`, `governance/health/storage_tier_policy_latest.json`, `governance/health/storage_tier_offload_manifest_latest.json`, `.ok` markers | storage router for target ownership; lifecycle policy and planner for active-tail isolation, bounded compaction, tier candidates, backpressure advice, and proof-gated retirement review; the planner has no move, throttle, delete, order, or live authority |
| Storage and ingestion definition view | `scripts/ops/ingestion_data_contract.py`, `core/storage_router.py`, existing collector/lane/lifecycle owners | `data_plane_definition` in `ingestion_storage_control_latest.json`, `ingestion-storage-control --definitions-only --json` | definition view reads owning policies and bounded per-path metadata; configured intent, physical route, fetch success, event usability, SQL checkpoint, merge and retirement proof remain separate; no directory-wide integrity, daemon-environment, exactly-once, mutation or execution authority is claimed; see `docs/architecture/STORAGE_AND_INGESTION_CONTRACT.md` |
| Adaptive unattended cold-archive routing | `scripts/ops/soak_self_healing_control.py`, protected-volume policy, and the storage router | cold-archive route state in self-healing evidence and deferred local queue under `local_fallback_storage/cold_archive_deferred` | an unavailable external cold root cannot block the hot path; the controller routes new cold work to a bounded readable local deferred target, reports redundancy debt honestly, and automatically returns to the configured external target when it is available; protected volumes remain denied |
| CPU workload classes and Apple Silicon scheduling intent | `config/cpu_workload_policy_v1.json`, `core/cpu_workload_policy.py`, `scripts/ops/apple_silicon_profile.py`, `scripts/ops/load_runtime_env.sh`, and explicit launcher markers | `governance/health/apple_silicon_profile_latest.json`, runtime-throttle process classification, and `[RuntimeCPU]` startup rows | policy and resolver first; live/paper/decision work uses locked priority ceilings and a bounded critical shared pool, collection/research/storage uses priority floors and support spillover, one P core remains reserved on the current host, and macOS hard affinity is never claimed; a currently over-niced critical process reports managed-restart debt without restarting the soak or changing execution authority |
| Local storage reserve and recovery | `core/local_storage_reserve.py`, `scripts/ops/local_storage_reserve_guard.py`, `scripts/ops/sqlite_reclaim_control.py`, `scripts/ops/sqlite_maintenance_launchd.sh` | `governance/health/local_storage_reserve_guard_latest.json`, `governance/health/sqlite_reclaim_control_latest.json`, reserve override and bounded history | reserve policy and guard together; warning and recovery thresholds use hysteresis, physical and occupied SQLite pages remain distinct, material free-page reclamation requires writer ownership plus memory and scratch reserves, scheduled retries obey the quiet-window guard, and protected volumes fail closed; see `docs/operations/SOURCE_AND_STORAGE_MAINTENANCE.md` |
| Production resilience framework | `config/production_resilience_v1.json`, `scripts/ops/production_resilience_control.py` | `governance/health/production_resilience_control_latest.json` with separate implementation, paper-soak, and live-promotion verdicts | resilience manifest and evaluator together |
| Institutional capability, source-count, entitlement, and evidence-gap contract | `config/institutional_capability_control_v1.json`, `scripts/ops/institutional_capability_control.py` | `governance/health/institutional_capability_control_latest.json`, `[institutional-capabilities]` livefeed row | policy and evaluator together; six pillars publish separate implementation, guarded-paper, current-candidate evidence, conditional-entitlement, and live-promotion states; 15-30 authoritative provider families are a planning target rather than a grade, derived features are not new sources, optional depth does not block healthy paper collection, and subscriptions, independent fills, attestations, strategy admission, and live release cannot be self-issued |
| Authoritative system influences and deep structural controls | `config/authoritative_systems_v1.json`, `core/authoritative_systems.py`, the 18 named control owners, and `scripts/ops/authoritative_systems_control.py` | `governance/health/authoritative_systems_control_latest.json`, `[authoritative-systems]` livefeed row, CI structural floor | registry and named owner first; 39 official references map to 18 locally executable controls, while native ITCH/OUCH access, production snapshot adoption, TLC receipts, signed trusted provenance, external CDM interoperability, independent pricing observations, candidate portfolio outcomes, research-data-platform outcomes, institutional-extension outcomes, and optional Great Expectations runtime use remain separate from synthetic structural probes; every deep influence is paper/advisory-only and has no order, promotion, or profitability-claim authority |
| Canonical research data, entitlement, point-in-time, alpha lifecycle, source-value, simulation, SLO, and reproducibility contract | `config/research_data_platform_v1.json`, `core/research_data_platform.py`, `core/collector_capability_routing.py`, `core/institutional_decision_flow.py`, `scripts/ops/research_data_platform_control.py` | `governance/health/research_data_platform_control_latest.json`, its markdown report, catalog receipts in sleeve routes and decision traces, `[research-data-platform]` livefeed row, and self-model awareness | catalog and permitted-use policy first; ten bounded contracts cover 10 data products and all 15 decision families, point-in-time and candidate-outcome products are attached to every route, restricted new consumers fail closed without entitlement and terms-review receipts, source count never becomes source value, and implementation remains separate from candidate-bound evidence, profitability, promotion, or order authority |
| Institutional research extensions | `config/institutional_research_extensions_v1.json`, `core/institutional_research_extensions.py`, `core/collector_capability_routing.py`, `core/institutional_decision_flow.py`, `scripts/ops/institutional_research_extensions_control.py` | `governance/health/institutional_research_extensions_control_latest.json`, its markdown report, decision-route metadata receipts, `[institutional-research-extensions]` livefeed row, and self-model awareness | eight advisory contracts join independent factor benchmarks, incident ownership, material-change governance, candidate risk schedules, speed-cost frontiers, checkpointable research DAGs, immutable dataset versions, and cross-engine valuations; ten official Point72/Cubist, AQR, Man AHL, Two Sigma, D. E. Shaw, and Goldman references are design provenance only, ArcticDB adoption remains license-gated, structural `8/8` is separate from earned candidate/external evidence, cumulative soak history is preserved, affected scopes accrue forward, and the layer has no signal, sizing, order, promotion, or profitability-claim authority |
| Investor readiness and evidence package | `config/investor_readiness_v1.json`, `scripts/ops/investor_readiness_control.py` | `governance/health/investor_readiness_control_latest.json`, the operator packet, paper/hypothetical tear sheet, and content-hashed data-room index | manifest and evaluator together; implementation, organic candidate/live evidence, and external attestations remain separate, missing documents stay missing, the `$200` canary is validation-only, future deposits never auto-scale weight, and this layer has no order, allocation, promotion, marketing, legal-approval, or self-attestation authority |
| Critical control ownership | `config/control_surface_ownership_v1.json`, `scripts/ops/control_surface_ownership.py` | `governance/health/control_surface_ownership_latest.json` and source receipts | ownership registry before adding or moving a writer |
| System roles, authority, and mutable state domains | `config/system_role_contracts_v1.json`, `core/system_role_contracts.py` | `governance/health/system_role_contract_latest.json`, runtime action decisions, and sensitive-action leases | role catalog before changing a component purpose, writer, order authority, escalation route, or resource boundary; unknown and ambiguous mutations fail closed |
| Unattended self-healing | `scripts/ops/soak_reliability_sentinel.py`, `scripts/ops/soak_self_healing_control.py` | sentinel, heavy-repair request, cooldown, circuit, and action-audit artifacts | lightweight sentinel for always-on checks; heavy controller only for bounded maintenance |
| Continuous-soak accounting | `scripts/ops/continuous_soak_integrity_control.py`, `governance/runtime/production_candidate_state.json`, `governance/evidence/production_candidate_events.jsonl` | `governance/health/continuous_soak_integrity_control_latest.json` with a headline cumulative main-soak count, separate clean promotion credit, observed current-window time, segmented history, and per-scope ages | include accepted pre-reset segments in the main soak count without relabeling candidate-event coverage as heartbeat proof; only the current unchanged candidate window may satisfy the clean `720`-hour promotion gate |
| Candidate source-integrity drift | `config/production_excellence_v1.json`, `scripts/ops/production_excellence_control.py`, `scripts/ops/source_mutation_guard.py` | per-file manifests in candidate state and events, `governance/health/source_mutation_guard_latest.json`, production-excellence candidate details, pre-commit result, and system-drift mesh row | the declared runtime-source inventory must have complete scope coverage; current and accepted manifests detect additions, modifications, and deletions, legacy candidates use an explicitly labeled Git fallback, clean evidence receives zero credit during drift, and no autopilot may accept a source change or mutate the event chain |
| Readiness and dashboard freshness | `scripts/ops/readiness_evidence_refresh.py`, `scripts/ops/run_production_hardening_watch_launchd.sh`, `scripts/ops/runtime_artifact_refresh.py` | profile-aware refresh report, runtime refresh epoch, dashboard freshness artifacts | 15-minute `accrual` cadence for organic evidence, 45-minute `production` cadence under a 60-minute freshness SLO for all ten pillar owners plus storage, live-feed, project, governance-drift, architecture, and infrastructure-supervisor producers; the profile republishes the self-model and architecture graph after supervisor convergence, while the bounded `dashboard` profile serves reads, a shared bounded data-plane connection serves collector enrichment, expected evidence-blocked and promotion-holdout outcomes stay separate from operational failures, and a generation-locked `training-profitability` epoch prevents mutable latest artifacts from interleaving; every unattended profile is live-locked |
| Grade regression repair | `scripts/ops/grade_regression_guard.py`, `scripts/ops/grade_regression_autopilot.py` | `governance/health/grade_regression_guard_latest.json`, `governance/health/grade_regression_autopilot_latest.json` | repair only non-ready surfaces; a healthy pass is a no-op and may not invoke the full runtime artifact graph |
| Live order state truth | `core/live_order_ledger.py` | `governance/health/live_order_ledger_control_latest.json`, SQLite quick-check, WAL/full-sync, event-chain, and materialized-state proof | ledger transaction and reconciliation contract |
| Supervised live-canary account preflight | `config/live_canary_micro_policy_v1.json`, `core/live_canary_preflight.py`, `scripts/ops/live_canary_preflight.py`, `scripts/ops/schwab_account_hash_keychain_sync.py`, `scripts/ops/load_runtime_env.sh`, `docs/operations/LIVE_CANARY_RUNBOOK.md` | operator-verified last-four aliases bind opaque Schwab routing hashes to owner Keychain entries; the live profile selects the designated policy hash in memory; short-lived owner-only operator attestation and candidate/account-bound allowlist remain under `governance/runtime`, with account, risk, order-ledger, release-manifest, tax, and exchange-session receipts inside the sealed envelope | plan, alias registry, and pure evaluator first, then Keychain sync and the explicit operator CLI; raw account numbers and hashes never enter tracked config or health artifacts, the selected hash must match the designated account policy and route, broker-visible cash plus human-confirmed settled cash must meet the cap, restrictions and provider balance fields require review, evidence must be fresh, and neither account binding, the receipt, attestation, nor allowlist can arm execution |
| Schwab-connected read-only canary dress rehearsal | `scripts/ops/live_canary_dress_rehearsal.py`, `config/live_canary_micro_policy_v1.json`, account-position truth, live-canary preflight, and the sealed-envelope contract | `governance/health/live_canary_dress_rehearsal_latest.json` with compact Schwab quote provenance, designated-account digest, exact redacted one-share order preview, settled-cash and post-fill projection, covered-position collateral proof, and reconciliation expectations | every live runtime switch is forced off before authentication; account truth and quote reads may contact Schwab but place, cancel, replace, paper-order, promotion, and live authority are impossible; provider margin fields remain diagnostic and never become cash or debt claims, raw account references and provider payloads are not persisted, and `ok` means the read-only rehearsal completed while `canary_ready` remains a separate fail-closed economic and release result |
| Post-canary closeout and graduation | `config/live_canary_graduation_v1.json`, `core/live_canary_graduation.py`, `core/live_canary_allowlist.py`, `scripts/ops/live_canary_closeout.py`, `scripts/ops/live_canary_graduation.py` | owner-only append-only `governance/evidence/live_canary_closeout_receipts.jsonl`, `governance/health/live_canary_closeout_latest.json`, and `governance/health/live_canary_graduation_latest.json` | one allowlist may admit at most one new entry while verified reduce-only exits remain available; closeout evidence requires the durable terminal order event, exact account/route/candidate identity, fresh post-fill account position and isolated cash deltas, cost evidence, source-backed regime context, and no safety ambiguity; round trips, independent days, post-cost expectancy, fill fidelity, drawdown, loss streak, fee, confidence-bound, and benchmark gates can make a later stage or capital tier review-eligible but can never progress it or authorize an order automatically |
| Candidate-bound sleeve scalability, organic capital growth, and application selection | `config/sleeve_scalability_selector_v1.json`, `core/sleeve_scalability_selector.py`, `scripts/ops/sleeve_scalability_selector.py` | `governance/health/sleeve_scalability_selector_latest.json`, candidate-bound bot profitability manifest, live-canary graduation and reconciled post-cost metrics, account capability truth, execution calibration, and profitability firewall | the selector aggregates only organized market-signal bots into sleeve evidence, requires positive conservative post-cost support, persistence, independent contribution, current-regime fit, drawdown control, calibrated capacity, candidate/account/route compatibility, and fresh sources, then searches a bounded low-correlation subset for one or multiple sleeves; it separately tracks the original `$200` seed through twenty-two targets ending at a `$1,000,000,000` research ceiling, excludes deposits, unrealized P&L, unattributed income, and cross-account results from organic progress, proposes bounded profit reinvestment plus reserve, and optimizes a next-target sleeve plan with escalating capacity headroom; classified accounts share policy but keep isolated evidence and authority, host transfer excludes account identifiers, credentials, OAuth, and Keychain state and requires a fresh rebind, and professional/institutional operating classes fail closed until their independent controls exist; every weight/notional remains advisory with no allocator, reinvestment, stage, capital, account activation, host reactivation, allowlist, paper-order, live-order, or profitability-guarantee authority |
| Sealed live execution and restart recovery | `config/production_readiness_control_v1.json`, `core/live_execution_envelope.py`, `core/live_execution_controls.py`, `core/base_trader.py`, `core/live_order_ledger.py`, `scripts/run_execution_lane.py` | `governance/health/live_execution_rehearsal_control_latest.json`, `governance/health/live_transition_integrity_latest.json`, durable ledger events, broker reconciliation receipts, and execution-lane freshness rejects | policy and envelope first, then preflight, firewall, broker adapter, and ledger together; every non-emergency entry is sealed to one candidate, designated account policy, fresh account/preflight receipt, immutable release, affirmative risk decision, Schwab quote provenance, explicit limit order, tax review, and broker payload with a short expiry; whole-share stage-one orders are limited to the normal XNYS session outside auction buffers; broker mutations are dispatched at most once and ambiguous outcomes move to reconciliation instead of retry; replacement remains disabled and the unfilled deadline requires cancel plus reconciliation; rehearsal has no broker, network, paper-order, live-order, promotion, or profitability authority |
| Independent deadman monitoring | `scripts/observability_exporter.py` | `governance/health/independent_runtime_monitor_latest.json`, `exports/metrics/trading_system.prom`, optional off-host heartbeat response | standalone stdlib monitor and its LaunchAgent; locally healthy reserve `watch` is paper-safe, hard storage pressure remains blocking, and off-host delivery remains mandatory only for live promotion |
| Codex project guardrails | `AGENTS.md`, `scripts/ops/codex_project_guard.py` | `governance/health/codex_project_guard_latest.json`, `.githooks/pre-commit` | `AGENTS.md` and guard script together |
| System architecture docs | `docs/architecture/ADR-0001-system-source-of-truth.md` | generated system explainer reports | ADR files in this directory |
Rules:

- Do not hand-edit generated operator docs as the lasting fix. Update the generator, then run `./scripts/ops/opsctl.sh commands-hygiene --apply --json`.
- Decision logs and channel logs are evidence, not control knobs. Change the writer contract in `core/accountability.py` before changing downstream consumers.
- Market, operational, and research labels have different outcome authorities. Never substitute forward price direction for an infrastructure result or an in-sample research score for out-of-sample evidence.
- Broker truth is the account state authority. Sleeve decisions can be confident, but execution should still defer to broker-truth, risk, halt, and storage guard evidence.
- A token-ready flag is not enough after an auth epoch changes. Account, position, paper-truth, and breaker measurements from the prior epoch remain stale until rebuilt, and a long-lived broker client must rebind or recycle before it can certify current truth.
- Collection health and paper-execution health are separate truths. Safety-parking the executor must remain visible without causing the watchdog to restart healthy collectors or allowing the dashboard to report an unqualified green state.
- Report commands should open the best available artifact and degrade from PDF to HTML or markdown instead of silently doing nothing.
- Codex guardrails should block mixed-domain commits and separate-domain README/docs drift before GitHub publishing.
- A green paper-soak verdict is not a live-release verdict. The resilience control requires all ten production evidence sections for live promotion and never grants live execution authority.
- Control grades, raw economic grades, and candidate-bound promotion evidence are separate truths. No control may rewrite a weak raw result or imply guaranteed future profitability.
- Bot count is not an alpha metric. New bots require a named hierarchy cell, an evidenced capability gap, independent post-cost value, correlation clearance, resource capacity, and human registry admission.
- Collector count is not evidence quality. Every added collector needs a real producer, bounded freshness policy, payload lineage, named evidence domains, and zero live-order or automatic-promotion authority.
- Source count is not an institutional-readiness or alpha score. Prefer a compact strategy-declared set of primary, official, direct-market, and independently corroborating provider families; derived features retain their original source identity, and a public firm's 10,000-source scale is not a local requirement.
- Capability count is not process count or implemented coverage. Logical capabilities must reuse shared snapshots, every current physical collector and organized bot must map deterministically, and unsupported capabilities must remain explicit live-promotion debt rather than silently starting work or receiving readiness credit.
- Sleeve-master structure, current evidence quality, paper coordination readiness, and human live-review readiness are separate truths. The Grand Master may summarize or abstain, but it cannot manufacture evidence, profitability, an order, or a live release.
- Broad observation is not broad execution. Every non-deleted bot may collect, but paper fills require explicit versioned authority, market-signal identity, hierarchy mapping, quality floors, bounded cohort membership, and candidate binding; legacy paper flags never imply authority.
- Lifetime debt, current-day activity, all-candidate research flow, active-stage promotion flow, and active inventory are separate accounting truths. Carried inventory remains visible and reducible but cannot grade a new candidate; a recovery balance can clear only when authoritative active-book accounting and identity-bound post-cost candidate attribution agree.
- Protected or operator-reserved volumes are never implicit archive capacity. Cold archival needs an explicit non-protected route and must fail closed when none exists.
